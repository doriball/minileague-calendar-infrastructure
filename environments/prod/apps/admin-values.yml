image:
  tag: "dev-0.10"
service:
  type: ClusterIP
  port: 8083
springProfile: prod
mongodb:
  secretName: mongodb-secret
  uri: "mongodb-external-service.default.svc.cluster.local"
  port: 2717
  dbName: "calendar_prod"
admin:
  secretName: admin-secret