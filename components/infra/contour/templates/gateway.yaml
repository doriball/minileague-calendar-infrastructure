apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: contour-gateway
  namespace: contour
spec:
  gatewayClassName: contour-class
  listeners:
    - name: http
      protocol: HTTP
      port: 80